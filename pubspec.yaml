name: cbe_expense_tracker
description: An offline expense tracker for CBE SMS transactions
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.19.0
  flutter_riverpod: ^2.4.9
  telephony_fix: ^0.2.1
  permission_handler: ^11.0.1
  fl_chart: ^0.65.0
  url_launcher: ^6.2.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

